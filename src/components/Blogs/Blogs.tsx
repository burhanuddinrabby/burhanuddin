import React from 'react';

const Blogs = () => {
    return (
        <div style={{ textAlign: 'center', marginTop: '300px', fontWeight: 'bold' }}>
            Loading...
        </div>
    );
};

export default Blogs;